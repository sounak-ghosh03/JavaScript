"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // this cannot be executes as we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh") //good readability


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;


// number = 2 ^ 53
// bigint = for large numbers
// string = "assembly of characters"
// boolean = true or false
// null = stand alone value (type = object)
// undefined = kinda place holder for a value which is not defined yet (type = undefined)
// symbol = uniquness


console.log(typeof undefined); // undefined
console.log(typeof null); // object